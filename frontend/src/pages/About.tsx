import { Box } from "@mui/material";

export default function About() {
  return (
    <Box
      sx={{
        width: "300px",
        height: "300px",
        display: "flex",
        margin: "auto",
        alignItems: "center",
      }}
    >
      PAGE About
    </Box>
  );
}
