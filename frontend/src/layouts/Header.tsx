import { AppBar, Toolbar } from "@mui/material";


export default function Header() {
    return (
        <AppBar>
            <Toolbar>
                <div>Header</div>
            </Toolbar>
        </AppBar>
    )
}